# downloader
Crawls a given URL, recursively finds MP3/MP4/PDF files, and downloads them to a user-specified directory, mirroring the folder structure. Also supports direct YouTube (and any yt-dlp-compatible) URLs.
